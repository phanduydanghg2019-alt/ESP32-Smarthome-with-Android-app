Place your square PNG icon (preferably 1024x1024, transparent background) here named `icon.png`.

Then run the generator script from project root:

```powershell
python tools\generate_icons.py --source assets\icon.png
```

If you need to overwrite existing icons, add `--force`.
